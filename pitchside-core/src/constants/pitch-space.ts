/**
 * Canonical pitch card geometry for board + simulator (must match `pitchConfig.viewBox`
 * in `config/pitchConfig.ts`). Shared contract per ARCHITECTURE.md §4.1, §8.
 */
export const BOARD_PITCH_VIEWBOX = { w: 160, h: 100 } as const;
